import { NavLink, Route, Routes } from "react-router-dom";
import DiaryPage from "./pages/DiaryPage"; import ShoppingPage from "./pages/ShoppingPage"; import SettingsPage from "./pages/SettingsPage"; import AiPage from "./pages/AiPage";
const navigation = [{to:"/",label:"Дневник",icon:"⌂",end:true},{to:"/shopping",label:"Покупки",icon:"✓"},{to:"/ai",label:"Помощник",icon:"✦"},{to:"/settings",label:"Профиль",icon:"◉"}];
export default function App(){return <div className="app-shell"><header className="topbar"><div className="brand-mark">🍋</div><div><h1>Food Diary</h1><p>Питайтесь осознанно, без лишней сложности</p></div></header><main className="content"><Routes><Route path="/" element={<DiaryPage/>}/><Route path="/shopping" element={<ShoppingPage/>}/><Route path="/ai" element={<AiPage/>}/><Route path="/settings" element={<SettingsPage/>}/></Routes></main><nav className="tabs" aria-label="Навигация">{navigation.map(({to,label,icon,end})=><NavLink key={to} to={to} end={end}><span className="nav-icon">{icon}</span><span>{label}</span></NavLink>)}</nav></div>}
