import {useEffect,useState} from "react";
import {NavLink,Route,Routes} from "react-router-dom";
import {api} from "./api/client";
import DiaryPage from "./pages/DiaryPage";import ShoppingPage from "./pages/ShoppingPage";import SettingsPage from "./pages/SettingsPage";import AiPage from "./pages/AiPage";import AnalyticsPage from "./pages/AnalyticsPage";import ClinicianWorkspace from "./pages/ClinicianWorkspace";import Onboarding from "./pages/Onboarding";import BrandLogo from "./components/BrandLogo";
export default function App(){const [doctorMode,setDoctorMode]=useState(false),[ready,setReady]=useState<boolean|null>(null),[pending,setPending]=useState(false);useEffect(()=>{const accent=localStorage.getItem("fooddiary-accent");if(accent)document.documentElement.style.setProperty("--accent",accent);api.profile().then(p=>setReady(p.profile_completed)).catch(()=>setReady(true));api.careContext().then(v=>{setDoctorMode(v.role==="CLINICIAN"&&localStorage.getItem("fooddiary-doctor-mode")==="1");setPending(v.links.some(x=>x.status==="PENDING"))}).catch(()=>undefined)},[]);const nav=doctorMode?[["/","Пациенты","◉"],["/analytics","Аналитика","▦"],["/ai","Ассистент","✦"],["/settings","Профиль","◊"]]:[["/","Дневник","⌂"],["/shopping","Покупки","✓"],["/analytics","Аналитика","▦"],["/ai","Помощник","✦"],["/settings","Профиль","◊"]];return <div className="app-shell"><header className="topbar brand-centered"><BrandLogo/><div><h1>Food Diary</h1><p>{doctorMode?"Кабинет специалиста":"Питание с ясностью, без лишней сложности"}</p></div></header>{ready===false?<Onboarding done={()=>setReady(true)}/>:<><main className="content">{pending&&!doctorMode&&<NavLink className="care-notice" to="/settings">Запрос врача ожидает вашего подтверждения <span>Открыть →</span></NavLink>}<Routes><Route path="/" element={doctorMode?<ClinicianWorkspace view="patients"/>:<DiaryPage/>}/><Route path="/shopping" element={doctorMode?<ClinicianWorkspace view="patients"/>:<ShoppingPage/>}/><Route path="/analytics" element={doctorMode?<ClinicianWorkspace view="analytics"/>:<AnalyticsPage/>}/><Route path="/ai" element={doctorMode?<ClinicianWorkspace view="assistant"/>:<AiPage/>}/><Route path="/settings" element={<SettingsPage/>}/></Routes></main><nav className="tabs" aria-label="Навигация">{nav.map(([to,label,icon])=><NavLink key={to} to={to} end={to==="/"}><span className="nav-icon">{icon}</span><span>{label}</span></NavLink>)}</nav></>}</div>}