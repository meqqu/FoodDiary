import {useEffect,useState} from "react";
import {api,ShoppingItem} from "../api/client";
const errorText=(e:unknown)=>e instanceof Error?e.message:"Не удалось выполнить действие";
export default function ShoppingPage(){const [items,setItems]=useState<ShoppingItem[]>([]),[text,setText]=useState(""),[advice,setAdvice]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");const load=async()=>{try{setItems(await api.shopping())}catch(e){setError(errorText(e))}};useEffect(()=>{void load()},[]);const run=async(fn:()=>Promise<unknown>)=>{try{setError("");await fn();await load()}catch(e){setError(errorText(e))}};const dictate=()=>{const Ctor=(window as any).SpeechRecognition||(window as any).webkitSpeechRecognition;if(!Ctor){setError("Диктовка не поддерживается в этом браузере. В Telegram откройте приложение в Chrome или Safari.");return}const r=new Ctor();r.lang="ru-RU";r.interimResults=false;r.onresult=(e:any)=>setText((v:string)=>`${v} ${e.results[0][0].transcript}`.trim());r.onerror=()=>setError("Не удалось распознать речь.");r.start()};return <><section className="card form-card"><div className="section-head"><div><strong>Умный список покупок</strong><span>Напишите или надиктуйте всё одной фразой — ИИ сам разложит по позициям</span></div></div><label>Что нужно купить?<textarea rows={3} value={text} onChange={e=>setText(e.target.value)} placeholder="Например: курицу, два авокадо, молоко и гречку"/></label><div className="row"><button className="btn" type="button" onClick={dictate}>🎙️ Надиктовать</button><button className="btn primary" disabled={busy||!text.trim()} onClick={async()=>{setBusy(true);try{setAdvice((await api.addShoppingSmart(text)).reply);setText("");await load()}catch(e){setError(errorText(e))}finally{setBusy(false)}}}>{busy?"Разбираю…":"Добавить с ИИ"}</button></div>{advice&&<div className="bubble">{advice}</div>}<button className="text-btn" disabled={busy} onClick={async()=>{setBusy(true);try{setAdvice((await api.refreshShoppingAdvice()).reply);await load()}catch(e){setError(errorText(e))}finally{setBusy(false)}}}>Обновить список под мои цели</button></section>{error&&<div className="error">{error}</div>}<section className="card"><div className="section-head"><strong>Список</strong><span>{items.filter(i=>!i.checked).length} осталось</span></div>{items.length===0?<div className="empty-state"><span>🛒</span><p>Попробуйте: «завтрак на три дня и фрукты».</p></div>:items.map(item=><div className={`check ${item.checked?"done":""}`} key={item.id}><input type="checkbox" checked={item.checked} onChange={()=>void run(()=>api.patchShopping(item.id,{checked:!item.checked}))}/><div style={{flex:1}}><div className="name">{item.name}</div><div className="muted">{item.category}{item.quantity?` · ${item.quantity}`:""}</div></div><button className="delete-btn" onClick={()=>void run(()=>api.deleteShopping(item.id))}>×</button></div>)}{items.some(i=>i.checked)&&<button className="btn" style={{marginTop:12}} onClick={()=>void run(()=>api.clearChecked())}>Удалить купленное</button>}</section></>}

